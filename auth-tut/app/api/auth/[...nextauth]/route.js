import { GET, POST } from "@/auth";

// Since you're importing `GET` and `POST` directly from `@/auth`, there's no need to reassign them.
// If your intention was to export them again, you can directly export them:

export { GET, POST };
